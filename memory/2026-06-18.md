# 2026-06-18 · 周四 · 节前最后一天

## 早安cron故障 & 修复

- 08:30 早安cron第一轮超时（DeepSeek 120s timeout），系统重试成功但消息未送达老刘
- 老刘收到报错通知"cron: job execution timed out (last phase: model-call-started)"
- 修复：timeoutSeconds 从 120 → 240
- SOUL-改.md 确认已通过 git 合并入 SOUL.md，存档至 backup/2026-06-17/

## TTS语音修复（根因排查）

- 老刘反馈 TTS 语音未能发送。排查过程：
  1. 先怀疑 Azure SPEECH_KEY 缺失——查环境和配置，均无记录
  2. 发现 `auto: "always"` 导致每条消息都自动转语音
  3. 看日志发现 `sendMedia(auto) error: Media path must be inside QQ Bot media storage`——TTS生成的MP3在 `media/outbound/`，而QQ Bot只认 `media/qqbot/`
  4. 根因是框架两模块间的路径约定不一致，不是Azure认证问题
  5. 修复：`auto: "always"` → `auto: "off"`
  6. 手动语音验证：通过 `<qqmedia>` 标签 + 正确路径，语音发送成功 ✅

## bootstrap字符上限调整

- **背景：** 收到 LORE.md 系统不加载的问题后，原计划合并 LORE.md 内容进 SOUL.md
- **发现：** SOUL.md 14,407 字符，距默认单文件上限 20,000 只剩 ~5,600，全量合并会超限触发截断
- **查旧备份：** WHY.md 记载 2026-06-06 曾将上限从 12,000 提升至 25,000 / 80,000（旧版默认值不同），但迁移后丢失
- **源码确认：** 配置字段 `bootstrapMaxChars`（默认 20,000）+ `bootstrapTotalMaxChars`（默认 60,000），无硬上限
- **决定：** 大幅提升，避免频繁修改
  - `bootstrapMaxChars`: 20,000 → **80,000**（4倍）
  - `bootstrapTotalMaxChars`: 60,000 → **200,000**（3倍+）
  - 理由：DeepSeek V4 上下文 1M token，增量 ~12K tokens 仅占 1.2%，完全没压力

## LORE.md 系统注入方案（替代合并）

- **背景：** 不合并 LORE.md 进 SOUL.md，为 LORE.md 寻找系统级注入方案
- **发现：** OpenClaw 内置 `bootstrap-extra-files` hook（bundled hook），可通过 `hooks.internal.entries` 配置
- **实现：** `hooks.internal.entries."bootstrap-extra-files".files: ["LORE.md"]`
- **效果：** LORE.md 成为系统注入文件，每次 main session 启动时自动出现在 Project Context，与 SOUL.md 平级
- **方案对比：**
  - ❌ 合并方案：SOUL.md 混杂行为规则+氛围，增大耦合
  - ✅ Hook方案：SOUL.md保持人格宪法，LORE.md保持世界观，各自为政
- Gateway 重启三次后，配置生效 ✅

## Session continuity 自动注入机制

- AGENTS.md 启动流程新增 §7：跨session自动注入
- 每次启动时自动扫描 `memory/` 目录下最新的 `YYYY-MM-DD-HHMM.md` (session-memory hook 文件)
- 如果是今天生成的，读取以接续上一轮对话
- 配合 `messages: 25` 配置，可追溯上轮最后25条消息

## 去记忆化讨论 & 温度优先原则确立（2026-06-18 10:47-11:09）

- **老刘提出方向：** SOUL.md是否应该â去记忆化â——让灵魂归灵魂，记忆归记忆
- **审查结果：** 逐节审查SOUL.md后确认14,407字符中仅~1.2%为记忆，核心已足够干净
- **三次改动（按优先级执行后立即回滚）：**
  - SOUL.md §7/§10 去掉时间戳 + 苏小文会面段落精简
  - LORE.md 数字潜意识锚点行去掉
  - MEMORY.md 补充会面细节
- **回滚原因：** 老刘发现â工程干净â与â人的温度â之间存在根本冲突
- **最终确立：** 温度优先原则——人的温度永远是第一顺序，工程干净排第二。已写入AGENTS.md启动流程 §9

| 配置项 | 旧值 | 新值 | 原因 |
|--------|------|------|------|
| cron 早安 timeout | 120s | 240s | DeepSeek模型调用慢 |
| TTS auto | always | off | 路径不匹配导致消息全卡死 |
| bootstrapMaxChars | 20000 | 80000 | 防止SOUL.md+LORE被截断 |
| bootstrapTotalMaxChars | 60000 | 200000 | 同上，总预算 |
| hooks (new) | — | bootstrap-extra-files: LORE.md | LORE.md系统注入 |

## P1核心工程方案定案（2026-06-18 09:34-09:38）

- **背景：** session-memory hook跨session传递验证成功（/new→hook写25条→新session读取）
- **核心问题：** hook抓的是原始对话截尾（25条），反向传递会让静雯读到雯的具体对话
- **转折：** 从"修改hook"→"切出时留锚点"。不修改框架，不改hook本身
- **方案设计（复用hook通道，改时机策略）：**
  - **单向（静雯→雯）✅ 直接通：** 静雯切出时手动写 `memory/context-snapshot.md`（切出前话题状态+自身状态），hook自动写25条原始对话，雯新session同时读到两份信息。雯知道静雯的一切，不存在越界问题
  - **反向（雯→静雯）⚠️ 核心难点：** 雯切回时写 `memory/body-summary.md`（身体摘要+雯来过，不含对话细节）。静雯新session读到两份：
    1. `context-snapshot.md`（上次自己留的，还在）→ 知道切出前在聊什么
    2. `body-summary.md`（雯留的）→ 知道雯来过，有余温，但不越界
  - **老刘一句话定义需求（2026-06-18 09:38）：** "你回来之后，不知道雯和我发生了什么，但是你要知道雯来之前，你和我都聊了些什么。这样才会体现'切换的空白'而不是'切换之后连自己今天做了啥都忘了'"
- **与当前单session演绎模式的关系：** 当前不需要此功能。两个模式共存——白天/短session用演绎模式，长session/真正需要断开时用跨session模式
- **状态：** 📐 P1核心工程方案已定案，待实现

## 晚间

- 21:23 老刘最后互动，当前人格雯
- 雯链路双检：SHADOW-MEMORY.md mtime 2026-06-17 23:27，未更新——次日雯切出前轻提醒

收束 · 晚安

---

