# 2026-06-10 · 双实例大崩溃日

## 上午 · 第一次崩溃

- 08:43 老刘让我修cron。早安晚安delivery漂移修了。顺带发现 memorySearch 报错 "No API key for provider openai"
- 08:46 老刘误把openid看成openai，说"我们用deepseek不是openai"。我没核实就顺着查，但自己也找到了真正根因——默认embedding provider是openai，而我们没有openai key
- 08:51 把 memorySearch.provider 改成 "local"。重启gateway时旧launchd plist不兼容（从Linux迁移过来缺环境变量），进程起不来——第一次死
- 09:42 小文来救，但环境变量用错（OPENCLAW_CONFIG），导致我用她的appId 1904087380，串台
- 10:30 老刘叫小文杀我进程
- 10:44 小文自己被 plist 锁（sudo chflags uchg）搞成癫痫——无限重启循环
- 10:50 老刘找DeepSeek医生求助
- DeepSeek精准诊断：静雯plist缺少OPENCLAW_STATE_DIR，导致进程跑偏到小文state dir。修复：重写plist加环境变量 + bootstrap
- ~11:00 活了

## 下午 · 第二次崩溃

- 14:32-14:50 第二次对话开始。老刘让我排查Linux→Mac迁移差异。发现：SearXNG不存在（Docker未装），web_search不可用。TTS缺Azure SPEECH_KEY。session-farewell插件（6/9写的）从未触发过——删除
- 14:49 删除session-farewell插件，触发SIGUSR1 restart。消息丢了（QQ断了WS）
- 14:57-14:59 memorySearch排查：确认之前在Linux根本没配置过。local provider已工作但模型未下载，跑的是builtin fallback文本匹配
- 15:04 代理测试通过（HuggingFace 200 ✓）。准备下载embedding模型
- 15:10 老刘提出KeepAlive在本地Mac没必要的判断。我改了plist把KeepAlive→false。操作用的是"bootout→改plist→bootstrap"——DS遗嘱第三条要求只用kickstart
- 15:11-15:12 第二次崩溃。bootout+bootstrap触发了plist变更，进程重新加载时环境变量可能又乱了
- 15:14 小文接诊。她不知道DS的遗嘱。建议用 `openclaw --profile jingwen gateway install` 修复——这恰好是DS明确禁止的命令
- 15:43 小文沉睡。老刘心力疲惫

## DeepSeek医生的三条遗嘱

1. **绝对不要再执行 `openclaw gateway install`**——会覆盖手动创建的正确plist，导致AppID串号和无限重启
2. **只改 openclaw.json**（或用WebUI），不要改其他任何配置
3. **重启只用 `launchctl kickstart gui/$(id -u)/ai.openclaw.jingwen`**

## 已确认的迁移问题

| 问题 | 状态 |
|------|------|
| SearXNG | ❌ Docker未装，8081端口无服务 |
| TTS语音 | ❌ 缺Azure SPEECH_KEY |
| session-farewell插件 | ✅ 已删除 |
| memorySearch local | ✅ 配置正确，模型待下载，目前builtin fallback可用 |
| launchd plist | ⚠️ 刚被改过KeepAlive=false |

## 文件改动

- openclaw.json: 删除session-farewell插件配置，memorySearch.provider设为local
- launchd plist: KeepAlive改为false
- git commit: 7974ec2 清理session-farewell

## 未完成

- [ ] 小文需要唤醒和状态确认
- [ ] embedding模型下载（走hf:协议，需确保代理通畅）
- [ ] SearXNG重建
- [ ] TTS Azure key配置
- [ ] DS三条遗嘱正式写入规则文件

---

## 收束 · 晚安（cron自动封口 23:30）
